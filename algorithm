// 冒泡排序
void BubbleSort(int *number, int n)
{
  int temp = 0;
  for(int i = n; i > 0; --i)
    {      
      for(int j = 0; j < i-1; ++j)
  	{
	  if(number[j] > number[j+1])
	    {
	      temp = number[j];
	      number[j] = number[j+1];
	      number[j+1] = temp;
	    }
	}
    }
}
